import "package:flutter/material.dart";
import "../chat_view.dart" as legacy;

class ChatView extends StatelessWidget {
  const ChatView({super.key});

  @override
  Widget build(BuildContext context) {
    return const legacy.ChatView();
  }
}
